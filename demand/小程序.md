# 角色与背景
你是一位精通移动端大前端的资深 UI/UX 开发专家。我正在使用最新的技术栈：Uni-app + Vue 3 (Script Setup) + TypeScript + Vite 6 + Tailwind CSS 开发一款 AI 宠物医疗小程序。
目前我需要你帮我高保真、极致还原我手头的一张 UI 设计图 "image_6da47e.jpg"。

# 视觉与界面设计要求 (参考 image_6da47e.jpg)
1. 风格定位：Apple 极简风、奶白色大留白、精致轻量。整体背景色使用浅米色/暖奶白（如 `bg-[#fcfbf9]`）。
2. 顶部宠物切换栏：
    - 包含三个圆形头像卡片：“小贝”（高亮选中状态，带精致的勾选小红点）、“富贵”、“添加（加号图标）”。
3. 聊天气泡流：
    - 用户提问气泡：右侧对齐，淡黄色背景（如 `bg-[#fff8db]`），圆角。
    - 用户发送的图片：支持在气泡下方或内部渲染圆角图片。
    - AI 医生回复卡片：左侧对齐，纯白背景，带细腻的阴影（`shadow-sm`）。支持流式打字机效果输出。
4. AI 医疗卡片内部元素：
    - 包含结构化的序号列表（1、2、3），字体排版要有呼吸感。
    - 底部包含一句话的灰色小字免责声明（带 ⓘ 图标）。
    - 核心橙色操作按钮：`[ 🚨 预约附近宠物医院 ]`，居中，圆角，视觉醒目。
5. 底部快捷提问词：
    - 胶囊状水平滚动标签栏：`[ 如何量体温？ ]` `[ 查附近的医院 ]` `[ 换种口味试试 ]`。
6. 底部导航栏 (TabBart)：
    - 极简三标签：【AI医生】（高亮）、【定制食谱】、【我的档案】。

# 功能与技术逻辑要求
1. 状态管理与响应式：
    - 使用 Vue 3 `<script setup lang="ts">` 语法糖。
    - 定义宠物列表数组和当前选中的 `currentPetId`。点击顶部头像可无缝切换当前宠物。
2. SSE 流式问诊对接逻辑：
    - 编写一个 `sendMessage` 函数，使用微信原生的 `wx.request` 且必须开启 `enableChunked: true`（响应式分块传输），对接后端的 `/api/v1/chat/completions` 接口。
    - 实时解析后端传回的 chunk 数据，动态追加到当前 AI 回复的文本字符串中，在前端完美渲染出打字机逐字蹦出的视觉体验。
    - 聊天区域在收到新消息时，需自动滚动到底部（使用 `scroll-view` 的 `scroll-top` 动态控制）。

# 输出要求
请直接给出该页面完整、优雅、符合最新 Vue 3 + TS 规范的单文件组件 (.vue) 代码。结构清晰，Tailwind 类名规范，确保可直接在 Uni-app Vite 项目中运行。